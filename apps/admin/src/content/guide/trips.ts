export const content = `
## Working with Trips

Trips are the core records in Tailfire. Every client journey — from initial inquiry to post-travel follow-up — lives inside a trip.

## Viewing Trips

The **Trips** page offers three views:

- **Kanban** — cards organized by trip status (Planning, Active, Travelling, Travelled). Drag trips between columns to update their status.
- **Table** — a sortable, filterable list of all trips with key details at a glance.
- **Group** — trips organized by trip group, useful for managing departures or series.

Switch between views using the toggle buttons at the top of the page.

## Creating a Trip

1. Click **New Trip** (or use the **+** button).
2. Enter the **Trip Name** — make it recognizable to your team.
3. Set the **Trip Type** if your agency uses types for reporting.
4. Enter **Start Date** and **End Date**, or choose **Add Dates Later** if dates are unknown.
5. Set the **Timezone** for the destination.
6. Assign a **Trip Group** if this trip belongs to a series or group departure.
7. Add **Tags** to organize the trip from the start.
8. Optionally select a **Cover Photo**.
9. Click **Save**. The trip opens immediately.

> **Tip:** Use a descriptive trip name your whole team will recognize — include the destination or client name. Avoid putting group info in the trip name; use trip groups instead.

## Searching and Filtering

- Use the **Search** bar to find trips by name, contact, or destination.
- Apply **Filters** by status, agent, date range, tags, or trip type.
- Save frequently used filter combinations for quick access.

## Trip Types

- **Regular trips** — standard client journeys (FIT, couples, families)
- **Group trips** — organized departures managed under a trip group with shared itineraries and coordinated bookings

> **Tip:** When dates are added to a trip later, Tailfire may automatically generate itinerary days based on the date range.
`
